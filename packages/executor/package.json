{
  "name": "@bioagent/executor",
  "version": "0.1.0",
  "private": true,
  "description": "Docker container lifecycle management — pull, run, exec, monitor bioinformatics images",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:unit": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dockerode": "^4.0.0",
    "pino": "^9.0.0",
    "undici": "^8.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
